[pytest]
addopts = -ra
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    acceptance: isolated Linux test against an ephemeral loopback OpenSSH server
    fido: explicit operator-assisted loopback test using a supplied FIDO identity
